# QA inventory

## User-visible claims

1. The home screen paints one UI-free wallpaper continuously across sidebar and main content, with a live native heading, the real project utility/composer surface, and any suggestion cards rendered by the current Codex host.
2. Sidebar, main area, header, and composer use coordinated readability layers; home remains expressive while normal task routes use a stronger quiet veil.
3. All real Codex controls remain interactive; the skin is not a screenshot overlay.
4. The skin survives route changes and renderer reloads while the injector daemon runs.
5. After closing every Codex process, the installed `Codex Kanna Blue Skin` desktop or Start-menu shortcut launches the managed runtime, recreates state, and reapplies the skin; the stock Codex shortcut is not treated as a themed launcher.
6. The official Store package and `app.asar` remain unchanged.
7. Restore removes the injected DOM/CSS and install/restore can be repeated.
8. Restore closes the saved CDP listener before reopening Codex normally.

## Functional checks

- Home feature card: click one card and confirm the real composer is populated or the normal action occurs.
- Project selector: click the real project chip under the "选择项目" label and confirm the native project menu opens.
- Account menu: open the real profile menu, activate its usage, pet, and settings items in turn, verify keyboard focus remains visible, then reopen it and confirm sign-out is still present without activating that destructive action.
- Sidebar: open a real task, then return to New Task.
- Sidebar modes: test the click-expanded inline sidebar separately from the edge-hover drawer. For the hover case, collapse the sidebar, move the real pointer outside the Codex window, then enter at the left edge and wait for the fixed `z-[42]` host containing a native `ASIDE` and `nav.sidebar-foreground-muted`. The outer host must own independent artwork and rounded low-fog glass while the inner aside is transparent; in dark mode the portrait/stage must remain recognizable rather than solid black. Preserve navigation clicks, scrolling, focus, and mode-safe text, then move to the main canvas and require automatic closure.
- Settings from the hover drawer: open the native profile menu inside the real edge-hover drawer and activate its settings item. After the drawer portal is destroyed, the settings page must keep `dream-settings-shell`, portrait artwork, and transparent nested surfaces rather than falling back to a white task canvas. The renderer may briefly use the native title-bar toggle and `Ctrl+,` menu item to recover the genuine inline sidebar; require the final route to remain Settings with one interactive native sidebar and no cloned navigation.
- Task side panel: open and close the native thread panel twice, resize the window, and repeat; the toggle must remain visible and clickable.
- Composer: type text, verify caret/readability, then clear it without sending.
- Composer file queue: attach or generate at least three file/change rows, confirm every filename and trailing action remains readable over portrait highlights, exercise one non-destructive row action, and verify the empty queue returns to the normal composer height.
- Queued follow-up: while a turn is running, queue a second message and confirm the real list carries the theme marker, its body, drag handle, steer/retry, delete, and overflow controls remain readable, and the activity rail does not carry that marker; exercise drag ordering with two queued messages and leave the queue empty afterward.
- Reload: use CDP `Page.reload`, wait, and confirm the injection marker returns.
- Pet overlay: open a desktop pet and confirm its auxiliary window stays transparent with no skin background or decoration layer behind it.
- Restore/reapply cycle: remove live skin, verify marker absent, apply again, verify marker present.
- Update resilience: resolve the current `OpenAI.Codex` Appx location dynamically for launch. A versioned path saved for cleanup must be revalidated against the registered package full/family identity before any process is stopped.
- Restart consent: an existing normal Codex window is never force-closed without explicit CLI authorization or shortcut confirmation.
- Shortcut policy: only visible launch and restore shortcuts are installed, both use `RemoteSigned` without `Bypass`, and no hidden PowerShell tray `.lnk` is created or auto-started. Internet-zone markers are removed only from hash-verified managed PowerShell copies.
- Config safety: Chinese project names, LF/CRLF choice, quoted target keys, table-header comments, and unrelated TOML sections survive install/selective restore; ambiguous target shapes fail unchanged, exact recovery keeps a copy of the replaced current file, and install refuses both registered and state-recorded old Codex processes.
- Theme safety: empty/over-16 MB images, over-16384px/50MP dimensions, path escapes, symlinks/junctions, malformed JSON, and unsupported formats are rejected before payload construction.
- Theme package/history safety: `.cdxtheme` import rejects directories, duplicate/extra entries, path traversal, unsupported schema versions, image extension/content mismatch, packages over 32 MB, expanded payloads over 64 MB, metadata over 256 KB, and individual images over 24 MB; imported themes receive a fresh package-owned ID. Export contains only `manifest.json`, `theme.json`, and the one to six declared images. Save/restore snapshots the prior JSON, rollback preserves the replaced current version, retention never exceeds 20 snapshots, and orphan cleanup preserves every image still referenced by current or retained history metadata.
- Optional manual tray lifecycle: when explicitly launched by the user, pause/resume reflects the clicked state and complete restore terminates it before it can reapply the skin; installation never launches it automatically.

## Visual checks

- 1280x820 initial home: the declared focus stays in frame, the text-safe side remains readable, the real project utility row and composer form one coherent surface, and no horizontal scrolling appears.
- Home ribbon: `BLUE MOMENT` remains inside the measured hero frame, never intersects the home composer rectangle or its project/placeholder/typed-text lane, stays non-interactive, and hides below 1120px.
- Narrower window: accept Codex's native responsive card reduction or omission; no essential control is covered and wallpaper cropping preserves the focus/safe-area contract.
- Normal task: the wallpaper is visibly quieter than home, messages keep high contrast, and composer does not overlap content.
- Inspect the sidebar, header, wallpaper edges, native card labels when present, project utility row, composer controls, scrollbar, dialogs, and menus.
- Account and option menus: the portrait must remain recognizable through the low-fog surface in both light and dark modes; nested rows may add only a restrained translucent hover layer, while every label and icon remains readable.
- Composer attachment/change queue: filenames, muted metadata, guidance labels, remove buttons, and overflow controls use fully opaque foreground tokens on a translucent strip; reject faded or dotted-looking labels without restoring a solid white block.
- Queued follow-up panel: message text and all actions remain fully opaque over the portrait-first layer in light and dark mode; nested Markdown has no inner bubble, padding, radius, or conversation-height override, at least 95% of its glyph box stays inside the native single-line viewport, and the dedicated surface does not suppress native drag/edit opacity feedback.
- Native material editor: schema-v1 themes load the established Kanna defaults; schema-v2 light/dark page, sidebar, composer, and card values round-trip within 4% to 92%; renderer custom properties match the saved draft; preview leaves `activeThemeId` unchanged; cancel restores the active payload and removes the package-owned temporary directory.
- Component material editor: select messages, summaries, task-hover previews, menus, workspace panels, code/diffs, and home suggestions independently. For each group, verify light and dark `#RRGGBB` tint and 4% to 92% opacity round-trip through schema 8, the miniature swatches update, reset affects only the selected group, and all 28 fixed renderer variables match the previewed draft. Reject unknown groups, malformed colors, and out-of-range opacity without changing the saved theme.
- Task-hover preview contrast: hover a sidebar task in light and dark modes. Both current root-owned and legacy nested dropdown structures must use the `previews` tint/opacity, while the title, age, project, branch, and icons retain readable mode-safe contrast. The dark card must never combine a pale background with white text.
- Native regional artwork editor: a schema-v8 theme can select different valid images for page background, sidebar, task composer, home hero/photo frame, home composer, and home Polaroid; all six thumbnails update without changing unrelated slots, undo/redo retains immutable staged files until save, saved JSON names six valid package-owned files, and a real export contains exactly those distinct images plus the manifest and metadata. Older schemas preserve their established fallbacks, including home composer following task composer and Polaroid following the home frame.
- Regional inheritance/discard: slots labeled as following the main background move to the new primary file when it changes, independent slots remain unchanged, each regional reset restores the shared file and disables itself, and confirmed draft discard removes only unreferenced staged images. A malformed current `theme.json` must abort cleanup before any image deletion; retained history references remain protected.
- Native theme interchange/history: export a saved preset, inspect the archive allowlist and manifest version, reimport it, verify the new local ID and byte-identical art, save two distinct edits, restore the newest snapshot, verify the replaced current version remains in history, then delete only the acceptance copy.
- Regional composition: for background, sidebar, task composer, home photo frame, home composer, and home Polaroid, change focus X/Y, zoom, fit, and both offsets; verify the true-ratio preview changes immediately, save/reload preserves schema-8 values, copy-main reproduces the background values, and restore-recommended returns the selected slot only.
- Source-image viewport: connect to the trusted Codex theme port and switch through all six regions. Confirm the complete original image remains visible at its own aspect ratio while a blue viewport uses the available route region's actual pixel ratio (with a clearly labelled adaptive reference only when that region is absent). Drag the viewport rather than the image, use Ctrl + wheel to resize it, click outside it to relocate it, and use arrow keys for fine movement. Plain wheel input over the canvas must continue scrolling the page. Confirm the four outside areas dim without covering the selected content, one drag adds one undo point, “fit current frame” produces `cover / 1x / 0 / 0`, and save/history/package operations never create a cropped raster file. The translated focus/zoom parameters must produce the same live CSS pixel size and position; cover/auto must never leave a blank edge.
- Viewport slot switching: rapidly switch from a wide region to the live `275×998` sidebar, including when both slots reuse the same already-decoded image object and no second `ImageOpened` event fires. The old frame must disappear, then reopen from cached `BitmapDecoder` dimensions at exactly `0.276:1`; on a 510×360 full-source preview at 1x cover this is approximately 99×360, never the prior region's width. Repeat after changing images to exercise the failure path.
- Light/dark miniature parity: verify all six preview content surfaces against live/fallback geometry. Sidebar must remain about `0.276:1`, task composer `7.51:1`, home frame `7.46:1`, home composer `9.39:1`, and Polaroid `0.772:1`; every Canvas-positioned image must remain visible. Confirm vertical sidebar/card glass, horizontal composer glass, the blue-left home mask, and the uncovered Polaroid in both preview modes.
- Regional image ownership: inspect computed styles on a normal task in both light and dark modes and after narrowing the window. Sidebar and main canvas each contain exactly one Blob-backed image with only `no-repeat`; the task composer base contains no Blob and its masked `::after` contains exactly one. With recommended automatic fit, the task-composer image computes to `cover` and reaches the rounded right edge behind the model/microphone/send controls. Reject tiled `repeat`, uncovered trailing strips, portrait seams, or duplicate body parts.
- Appearance preview: toggle light/dark without leaving the manager and verify sidebar, message, task composer, home photo frame, home composer, and Polaroid samples all update together. Confirm low contrast and glass below 12% produce visible warnings, while a readable configuration reports an estimated 4.5:1 pass.
- Automatic palette: analyze each selected regional image, verify the reported skin sample is excluded from color selection, and exercise fresh blue-white, midnight blue, and low-fog suggestions. Confirm each creates distinct bounded light/dark material values and never emits arbitrary CSS or a skin-red full-surface accent.
- Continuous live sync: enable the localized native toggle on an editable copy, change one composition value, and verify the live Codex CSS variable changes without activating Preview or Save. Issue at least three changes inside the 240ms debounce window and confirm only the final value is rendered, no stale error remains, unchanged preview image hashes/timestamps are preserved, and the manual Preview command is disabled while live sync owns the temporary session. Turn the toggle off and confirm the persisted active theme, early reload payload, and empty preview directory are restored without restarting Codex.
- Theme editing studio: verify the horizontal theme library remains at the top, followed by the live image/composition canvas and then the full-width property inspector. The page must keep this ordering at desktop and narrow widths without a nested outer card, dynamic surface reparenting, process exit, clipped commands, or horizontal content loss.
- Theme editor scroll ownership: the page exposes exactly one vertical scrollbar. Wheel/trackpad input over the image editor, composition controls, appearance previews, and material controls must advance the same page scroll without first exhausting a nested region. The theme library may expose only its horizontal shelf scrollbar.
- Full-width composition editor: the regional composition Expander, slot selector, live-size status, preview frame, sliders, and action row stretch to the current page width. At 1240px the preview must no longer retain the former 510–560px desktop cap; after resizing narrower, it must recompute from the host width without horizontal overflow. Height may remain bounded to keep portrait sources usable in the single page scroll.
- Responsive inspector sections: light/dark preview, automatic palette, visual accent picker, glass materials, and component-level advanced materials must match the inspector width. Preview cards retain the same two-column ordering at every supported width and scale within those columns; below 900px only the dense light/dark material editors become true single-column full-width rows. Palette actions divide the row evenly and no section may create horizontal scrolling.
- Stable composition and preview geometry: resize repeatedly across 900px. The regional composition controls and six appearance previews must not reorder, jump from two columns to one, or enlarge because of a breakpoint. Composition height follows width continuously with only a 520px maximum; no minimum-height plateau may distort a narrow canvas.
- Minimum manager window: native resize tracking and programmatic size changes must reject dimensions below 770×680. At exactly 770×680 the theme page remains responsive, exposes one vertical page scrollbar, and does not expose a horizontal page scrollbar.
- Reject black/transparent sidebar artifacts, clipped controls, duplicated/disconnected project labels, rasterized native controls, fake UI inside the wallpaper, weak contrast, or decorations intercepting clicks.

## Exploratory checks

- Start when the debug port is occupied: fail with a clear message or use a caller-selected port.
- Start after Codex updates: package discovery and injection still work without patching installed files.
- Tamper `state.json` with a reused PID: if the PID is still live but its identity differs, confirm cleanup fails closed and preserves `state.json`; if the PID is gone, confirm the stale record is replaced only after confirming no process is running, without stopping an unrelated process.
- Serve a fake `app://` CDP target or remote/mismatched WebSocket URL and confirm both launcher and injector reject it. Reuse the port with a new Browser ID and confirm the existing watcher exits without reconnecting.
- Force verification failure and confirm the injector, state file, and newly launched debug session are rolled back.
- Start two operations concurrently and confirm the second fails clearly without changing config, state, or processes.
- Close Codex without restore and confirm the Browser identity anchor closes and the watcher exits without reconnecting or rapidly growing logs.

## Automated checks

- `tests/run-tests.ps1`: strict UTF-8/no-BOM writes, UTF-16 rejection, LF/CRLF preservation, concurrent-write detection, exact backup/recovery, `[desktop]`-scoped restore, ambiguous TOML rejection, non-ASCII paths, Appx/state identity, argument quoting, theme seeding/import/save/switch/pause, schema-8 six-region composition and seven-group component-material validation, package/history contracts, byte/dimension limits, junction rejection, payload construction, Browser ID, loopback URL rejection, and renderer isolation for transparent auxiliary windows.
- `node --check` for the injector and renderer payload.
- Live Windows signoff remains required for Store process ownership, restart consent, screenshot, and CDP closure.
